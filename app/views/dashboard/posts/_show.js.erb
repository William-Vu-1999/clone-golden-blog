$('#wrap-comments-component').html(
"<%  @comment_paginate.each do |comment| %>"+
  "<%= j render partial: 'dashboard/comments/comment_item', locals: {comment: comment}%>"+
  "<%end %>"
);
$('#call-paginate-ajax').html(
"<%= j will_paginate @comment_paginate, inner_window: 2, outer_window: 1, :renderer => RemoteLinkPaginationHelper::LinkRenderer %>"
);

